#For this practice you need to make sure you have created webserver in workstation.
#install httpd package and start and enable the service
#then create materials folder inside /var/www/html directory and create motd directoy inside that materials folder. so your path should be /var/www/html/materials/motd
#copy the mmotd.ps1 file inside /var/www/html/materials/motd directory
